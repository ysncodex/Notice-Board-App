## Notice Board Client (Vite + React)

### Run locally

```bash
npm i
npm run dev
```

### Environment

Create `client/.env` from `client/env.example`:

- **`VITE_API_URL`**: backend base URL (example `http://localhost:5000`)

### Build (production)

```bash
npm run build
```
